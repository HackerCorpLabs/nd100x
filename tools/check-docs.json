{
  "_readme": [
    "Waivers for tools/check-docs.py, which reads this file from its own directory.",
    "",
    "A section that genuinely does not apply should be ABSENT, not filled with a",
    "placeholder - that is the first rule in the README template. This file is how you",
    "tell the checker so. Every waiver needs a reason: a waiver without one cannot be",
    "told apart from giving up.",
    "",
    "files   paths relative to the repository root. A bare name such as README.md also",
    "        matches any path ending in it; a folder with a trailing slash, such as",
    "        docs/, matches everything under it.",
    "checks  the check codes to waive for those files.",
    "reason  why. Required.",
    "",
    "Check codes: LINK ESCAPES ABSPATH MERMAID ALT-TEXT EMOJI-HEADING HAND-TOC",
    "             NEW-HEADING H1 BADGES NAVBAR INSTALL-ORDER LICENCE-LAST ABOUT STATUS",
    "             APPS-TABLE",
    "",
    "STATE OF THIS REPOSITORY, 2026-09-11: README.md passes every check. The waivers",
    "below cover the engineering notes, which were written before the standard existed",
    "and have not been brought onto it. They are DEFERRED, not approved - each entry",
    "says so. ABSPATH is deliberately not waived anywhere: every absolute path in this",
    "repository's own files was fixed rather than waived, because those leak the layout",
    "of a private machine into a public repository."
  ],

  "app_folders": [],

  "waivers": [
    {
      "files": ["docs/", "GLASS.md", "dap-analysis.md", "CONTRIBUTING.md"],
      "checks": ["EMOJI-HEADING", "HAND-TOC", "NEW-HEADING"],
      "reason": "Engineering notes and handoffs written before the standard. 767 emoji headings in docs/ alone; some of the emoji in those files carry status meaning inside tables and cannot be stripped blind. Deferred - a separate pass, file by file. Not approved as-is."
    },
    {
      "files": ["docs/", "src/", "GLASS.md", "dap-analysis.md"],
      "checks": ["MERMAID"],
      "reason": "95 diagrams written before the shared palette existed: pastel fills, style lines without stroke or color, forced themes. Re-colouring them is mechanical but each needs its diagram read to keep the meaning. Deferred. Not approved as-is."
    },
    {
      "files": ["MERMAID_COLOR_STANDARDS.md"],
      "checks": ["MERMAID"],
      "reason": "This repository's palette document is an OLDER, DIFFERENT document from the one shared by repo-standards, NDInsight and x25emu - it has a White row and forces theme base, which the shared standard bans outright. Reconciling the two is a decision, not a fix; until then this waiver marks the divergence rather than hiding it."
    },
    {
      "files": ["docs/cpu_documentation.md"],
      "checks": ["LINK"],
      "reason": "Generated by nd100-markdown from the nd100-definitions YAML. Line 386 links to addressing_modes.md, a page the generator does not emit, so the fix belongs in the generator's template, not in this file - editing it here would be overwritten on the next run."
    }
  ]
}
